<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.blogspot.salvadorhm.componentestic51"
    tools:ignore="MergeMarker">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_cardview" />
        <activity android:name=".Activity_Seek_Bar" />
        <activity android:name=".activity_spinner" />
        <activity android:name=".activity_progress_bar" />
        <activity android:name=".Activity_Tab_Layout" />
        <activity android:name=".Activity_Web_View" />
        <activity android:name=".Activity_Floating_Button" />
        <activity android:name=".Activity_Switch" />
        <activity android:name=".Activity_Toggle_Button" />
        <activity android:name=".Activity_Check_Box" />
        <activity android:name=".Activity_Radio_Button" />
        <activity android:name=".Activity_Video_View" />
        <activity android:name=".activity_image_button" />
        <activity android:name=".Activity_edittext" />
        <activity android:name=".Activity_Scroll_View" />
        <activity android:name=".Activity_Button" />
        <activity android:name=".Activity_Rating_Bar" />
        <activity android:name=".Activity_Map_View" />
        <activity android:name=".Activity_Calendar_View" />
        <activity android:name=".Activity_Image_View" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD1-cgHkcBQ--EFrtpLVtn7eTUW-m1tBGg" />

        <activity
            android:name=".MapsActivity1"
            android:label="@string/title_activity_maps1"></activity>
    </application>

</manifest>